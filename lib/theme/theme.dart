import 'package:flutter/material.dart';

const Color neonGreen = const Color(0xFF39FF14); // example
const Color backgroundMid = Color(0xFFEEEEEE); // Colors.grey[200] is not const, so use the value directly
const Color subtitleGray = Colors.grey;
const Color backgroundDark = Colors.black87;
const Color _textColor = Colors.black; // or appropriate color
const Color _primaryColor = Colors.blue; // or your theme color
const Color _cardColor = Colors.white; // or your card background